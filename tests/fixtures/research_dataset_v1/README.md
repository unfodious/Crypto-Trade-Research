# Research Dataset v1 Fixtures

These CSV files are fake, minimal fixtures for tests and contract examples only. They must not be used for research conclusions, model training, or live trading decisions.

The production research dataset should be generated as Parquet using the same fields described in `docs/data-contract.md`.
